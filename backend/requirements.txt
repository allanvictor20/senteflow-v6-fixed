# SenteFlow AI - Backend Dependencies (v6 — Groq + Deepgram + ElevenLabs)

# Primary LLM: Groq (OpenAI-compatible API)
groq>=0.11.0

# Fallback LLM providers (optional, configure via env vars)
anthropic>=0.40.0
openai>=1.50.0

# ── Voice note pipeline ──────────────────────────────────────────────────────
# Deepgram nova-2 — streaming/batch STT. Better Swahili/Luganda accuracy than
# Whisper, adds punctuation, ~3x faster. Used by services/llm/media_processor.py
deepgram-sdk>=3.7.0

# ElevenLabs — TTS for voice note replies. When the customer sends a voice
# note, the bot replies with a synthesized voice note (same channel, same
# modality). Used by integrations/whatsapp/reply_sender.py
elevenlabs>=1.0.0

# Silero VAD — segments long voice memos into separate utterances so a single
# 3-minute recap can produce multiple distinct business events. Used by
# services/llm/media_processor.py
silero-vad>=5.1.0

# Audio I/O — needed by Silero VAD to load/decode/convert WhatsApp audio
# files (ogg/m4a/mp3) into 16kHz mono PCM.
soundfile>=0.12.0
numpy>=1.24.0

# Firebase / Firestore
google-cloud-firestore>=2.19.0
firebase-admin>=6.5.0

# FastAPI server
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
websockets>=12.0

# Data validation
pydantic>=2.0.0

# Environment variables
python-dotenv>=1.0.0

# HTTP client — used by EvolutionClient and Sunbird language pipeline
httpx>=0.27.0

# PDF text extraction (replaces Gemini's native PDF support)
pypdf>=4.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

slowapi>=0.1.9
sentry-sdk[fastapi]>=2.0.0
prometheus-fastapi-instrumentator>=6.0.0

# Optional: required only if you re-enable the legacy Gemini Live realtime voice
# route (services/llm/gemini_live.py). This module is preserved as reference
# but no active route calls it — voice interaction now uses WhatsApp voice notes.
# google-genai>=1.0.0
